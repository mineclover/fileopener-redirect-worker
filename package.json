{
  "name": "fopen-redirect-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker to redirect HTTP URLs to fopen:// protocol",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "wrangler dev --local",
    "deploy": "wrangler deploy:dev",
    "deploy:prod": "wrangler deploy --env production",
    "preview": "wrangler preview",
    "tail": "wrangler tail"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "redirect",
    "fopen",
    "protocol-handler"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
